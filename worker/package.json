{
  "name": "vayana-enquiries-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that accepts /enquiries/ form submissions, verifies Turnstile, and appends a row to the Vayana Bungalows enquiries Google Sheet.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
